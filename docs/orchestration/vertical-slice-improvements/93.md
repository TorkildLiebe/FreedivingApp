## 2026-02-25T17:09:12Z (run-id: 20260225-163848-m3)

### What slowed execution
- Spot detail already exposed `diveLogs` in a placeholder shape, so contract-preserving migration to a paginated endpoint plus merged mobile fetch required extra mapping logic.
- Backend formatting/type gates required iterative fixes after adding pagination and preview mapping.

### Suggested process improvements
- Add a shared helper for paginated `page/limit` DTO + normalization to reduce repeated endpoint boilerplate.
- Add an M3-specific simulator capture utility for spot-detail list states to improve visual parity evidence quality.

## 2026-02-26T09:24:00Z (run-id: standalone)

### What slowed execution
- Prisma `migrate dev` drift checks on local Supabase extensions (beyond app tables) prevented direct use as a setup command and required migration strategy adjustment.
- Legacy migration chain had missing/partial files, so idempotent replay safety work had to cover both clean and pre-existing local database states.

### Suggested process improvements
- Split migration workflows explicitly in docs/scripts: one stable apply path (`migrate deploy`) for environment setup and one authoring path (`migrate dev`) for new schema changes.
- Add a migration integrity check in CI that validates all migration directories contain SQL and can be replayed from empty DB.
