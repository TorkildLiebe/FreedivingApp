# Milestone Retrospective — M2 (run 20260224-203327-m2)

## Outcome

- Milestone M2 completed with issues #73, #74, #75, #76, #77, and #78 implemented and committed on `codex/milestone/m2-map-spots`.
- All run artifacts were maintained incrementally and finalized to `completed` state.

## What worked well

- Per-issue plan/report contract stayed consistent, which improved resume reliability after interruptions.
- Hook-equivalent checks (`pre-edit`, `post-edit`, `pre-commit`, `post-test`) kept verification and reporting disciplined.
- Risk-tiered testing strategy caught architecture/lint boundary regressions early (notably map/auth import boundary for #77).

## Gaps observed

- iOS/Android runtime screenshot capture remained partially manual/limited in this environment for later issues.
- Some baseline mobile test warnings and lint warnings are unrelated but noisy, reducing signal during milestone runs.
- Current orchestration workflow writes issue states optimistically before commit in some interrupted paths, creating temporary run/git divergence.
- The process failed one time during auth because it didn't have credentials

## Recommended improvements

1. Add deterministic simulator automation for critical UI flows (open app, authenticate, navigate, capture screenshots) and expose as a scripted command.
2. Resolve persistent baseline warnings in mobile auth/map tests to keep CI and run logs high-signal.
3. Introduce a single script to atomically transition run-state + roadmap after successful commit to avoid premature `committed` state writes.
4. Add milestone-close helper that marks `merged_local` and `completed` in one guarded step after verifying all issue commits exist.
5. Add authentication so that the process can authenticate itself